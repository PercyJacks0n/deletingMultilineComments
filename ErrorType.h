#ifndef ERRORTYPE_H
#define ERRORTYPE_H

enum ErrorType
{
    invalidArgumentsCount,
    inputFileNotExist,
    outputFileCreateError,
    defineDirectiveFound,
    numberRowsExceeded,
    numberCharactersPerLineExceeded,
    invalidFileExtension
};

#endif // ERRORTYPE_H
